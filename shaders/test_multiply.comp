#version 450

layout(local_size_x = 256) in;

layout(set = 0, binding = 0) buffer OutputBuffer {
    uint data[];
} output_buf;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    output_buf.data[idx] = idx * 2;
}
