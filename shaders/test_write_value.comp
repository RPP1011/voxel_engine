#version 450

layout(local_size_x = 1) in;

layout(set = 0, binding = 0) buffer OutputBuffer {
    uint data[];
} output_buf;

layout(push_constant) uniform PushConstants {
    uint value;
} pc;

void main() {
    output_buf.data[gl_GlobalInvocationID.x] = pc.value;
}
